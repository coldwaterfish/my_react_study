### Math

```js
'随机数：Math.random()获取0~1之间的随机数(大于等于0小于1)',
    '获取n到m之间的随机数:Math.random()*(m-n)+n;',
    '绝对值：Math.abs(-1)',
    '向上取整：Math.ceil(1.2)',
    '向下取整：Math.floor(-1.1)',
    '四舍五入：Math.round(-1.51)如果是负数，临界点必须大于5',
    '开平方：Math.sqrt(9)',
    '取幂：Math.pow(3,2) ==> 9',
    'Math.PI',
    '最大值和最小值：Math.max(1,2,3)/Math.min(4,5,6)',
```

