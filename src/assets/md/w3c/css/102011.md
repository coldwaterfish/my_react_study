### 盒子模型

```css
'标准盒模型：content（内容区）+padding（填充区）+border（边框）+margin（外边距)',
'怪异盒模型：width{content（内容区）+padding（填充区）+border（边框）}+margin（外边距）',
'box-sizing：content-box标准,w3c标准盒模型（默认值）',
'box-sizing：border-box怪异，宽高包括border和padding,ie6盒模型'
```

